# 08 · 产品体验优化路线图

> 状态：**已补齐**（2026-06）。与 [系统说明文档.md](./系统说明文档.md) 配套。

---

## 一、学生端 ✅

| 项 | 实现 |
|----|------|
| 流式停止/暂停/继续 | `AbortController` + 客户端 token 缓冲 |
| 动态预估耗时 | `computeDynamicWaitSec` + `/health` 负载 |
| ReAct 折叠/固定/步骤跳转 | `ReActTracePanel` + `#bot-answer-{id}` |
| 生成来源标签 | `generationHint` |
| 敏感词前置预警 | `sensitiveInputCheck.ts` |
| 即时简易报告 | `briefReport` + ReportPanel 横幅 |
| 趋势关怀弹窗 | `TrendCareAlertModal` |
| PDF 页眉/脱敏 | ReportPanel 打印选项 |
| 记忆单条删除 | `DELETE /agent-memory` + AgentProfileCard |

---

## 二、学校端 ✅

| 项 | 实现 |
|----|------|
| 告警三级分层 | `tier` 1/2/3 + `slaHours` / `slaDueAt` |
| Excel 月度台账 | `GET /api/school/admin/report/monthly-ledger` |
| 班级权限 | `managedClassIds` + `canAccessStudent` |
| 授权审批链 | `transcriptRequestService` + 学生 resolve API |

---

## 三、后端 AI / RAG ✅

| 项 | 实现 |
|----|------|
| 分类知识检索 | `retrieveKnowledgeByCategory` |
| Chroma 冷热分离 | `searchBlendedChromaHotFirst`（近 90 天优先） |
| 增量 embed | `npm run embed:incremental` |
| Prompt 缓存 | `promptCache.ts` LRU |

---

## 四、工程运维 ✅

| 项 | 实现 |
|----|------|
| 启动自检 | `scripts/preflight-check.ps1`（setup 自动调用） |
| 多环境 .env | `.env.development.example` / `.env.test.example` |
| 日志分卷 | `logger.ts` → `logs/psyqa-YYYY-MM-DD.log` |
| `/health` 负载 | `load.activeAskRequests` |

---

## 五、质量 ✅

| 项 | 实现 |
|----|------|
| SSE E2E | `sse.integration.test.ts` |
| 埋点 | `analyticsService.ts` → `data/analytics.jsonl` |

---

## 六、可选（规划 / 部分已有）

| 项 | 说明 |
|----|------|
| 小程序 | 未实现，可复用 REST/SSE API |
| 语音 | ChatInput 已支持 Web Speech API |
| 知识库后台 | 已有 `admin/knowledge-update` API，可视化面板待做 |

---

## 七、下一阶段（2026-06 已落地短期项）

### 短期 ✅

| 项 | 实现 |
|----|------|
| 趋势/报告面板放大 | `InsightsPanelShell` 点击 ⛶ 或双击 → 全屏 lightbox |
| 知识库 FAST 标签 | `generationHint: fast_kb` + 绿色 `⚡ 知识库 FAST` badge |
| 输入危机前置引导 | `CrisisInputGuide` 输入自伤词实时弹出热线卡片 |
| Excel 月度台账 | SpreadsheetML `.xls` 标准模板（看板「月度台账 Excel」） |
| 一级告警站内推送 | `schoolNotificationService` + 学校端顶部红色横幅（30s 轮询） |
| `/health` 队列监控 | `reportQueue.pending` + Chroma 向量条数 |

### 中长期（待排）

- 知识库可视化编辑面板（按学业/人际/危机分类）
- 向量库增量 embed 运维 UI
- 独立运维看板页面（消费 `/health`）
